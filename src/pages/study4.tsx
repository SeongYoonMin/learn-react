import HookExam from "components/HookExam";

export default function study4() {
  /**
   * react hooks
   * Class Components에서 Function Components로 넘어오면서 Class Components에서 쓰던 method들을 사용하기 위해 만들어짐.
   * 조건문, 반복문에서 사용 불가 - hooks의 호출 순서에 이슈 발생
   */
  return (
    <>
      <HookExam />
    </>
  );
}
